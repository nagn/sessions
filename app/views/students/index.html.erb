<h1>All Students</h1>

<ul id="students">
  <% @students.each do |student| %>
    <li>
      <%= link_to student.name, student_path(student) %>
    </li>
  <% end %>
  <%= link_to "Create a New Student", new_student_path, class: "new_student" %>
</ul>
