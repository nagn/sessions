<%= form_for(@session) do |f| %>
  <h1>Attending Students</h1>
  <p>
    <script type="text/javascript">$(function() {
      /*
        var boxCloses = document.getElementsByClassName('boxclose');
        for (var i = 0; i < boxCloses.length; i++) {
          var boxClose = boxCloses[i];
          $boxClose.click(function(){
            alert("Closing");
          })
        }
        */
      
      $('.boxclose').click(function() {
        alert("closing box");
      });
    });
    </script>
    <style>
    .box{
          border: 2px solid;
          border-radius: 100px;
          background: #dddddd;
    }
    .boxclose{
      float:right;
      cursor:pointer;
      border: 5px solid #AEAEAE;
      border-radius: 30px;
      background: #605F61;
      font-size: 31px;
      font-weight: bold;
      display: inline-block;
      /*
      margin-top:-30px;
      margin-right:-30px;
      cursor:pointer;
      color: #fff;
      
      border-radius: 30px;
      
      font-size: 31px;
      font-weight: bold;
      display: inline-block;
      line-height: 0px;
      padding: 11px 3px;
      */
    }
    .boxclose:before {
      content: "×";
    }
    </style>
    <% @attended_students.each do |student| %>
    <div class="box" id="<%="student_box_#{student.id}"%>">
      <a class="boxclose" id= "<%="student_box_close_#{student.id}"%>"></a>
        <p><%= student.name %></p>
    </div>
    </p>
  
  <% end %>
<% end %>